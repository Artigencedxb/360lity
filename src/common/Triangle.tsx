import classNames from "classnames";
import React from "react";

const Triangle: React.FC<{ className?: string }> = ({ className }) => {
  return <div className={"trianglenew"} />;
};

export default Triangle;
